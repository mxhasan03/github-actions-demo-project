name : test project 

on: 
  pull_request: 
     brnaches:
        - main

jobs:
  test:
    runs-ons: ubuntu-latest
    container: 
      image: node:18
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: 18
      - run: npm ci
      - run: npm test